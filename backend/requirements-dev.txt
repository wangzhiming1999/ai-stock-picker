-r requirements.txt

# 测试依赖（开发/CI 用，刻意与运行时依赖分开）
#
# 为什么不写进 requirements.txt：Vercel 会按 requirements.txt 安装 Serverless 依赖，
# 而 akshare + pandas + numpy 已经很大，再塞 pytest 只会撑大部署体积。
#
# 背景：2026-09-16 之前本仓库有 19 个测试文件，但 requirements.txt 里没有 pytest、
# 也没有 pytest 配置，导致「测试跑不起来 / 换台机器无法复现」——测试等于不存在。
# 现在：`pip install -r requirements-dev.txt` 后直接 `pytest` 即可。
pytest==9.0.2
pytest-asyncio==1.3.0

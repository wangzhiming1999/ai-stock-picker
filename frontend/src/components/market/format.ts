/**
 * 两根温度带共用的纯格式化函数（与渲染分离，便于 node:test 直接跑）。
 */

/**
 * 带符号百分比。负期望走琥珀 —— 红绿只表达方向，收益读数（质量语义）不占。
 * 涨停溢价读数区块与跌停修复区块都会用到同一套带符号约定，抽到这里避免各写一遍。
 */
export function signedPct(v: number): string {
  return `${v > 0 ? "+" : ""}${v.toFixed(2)}%`;
}
